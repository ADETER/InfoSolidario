<%@recurso.each do |l| %>
<div class="text-center">
<div class="row">
		<div class="col-sm-4">
			<%= image_tag l.avatar.url, class: "img-responsive center-block", width: "30%", height: "30%" %>
		</div>	
		<div class="col-sm-4">
			<%=l.description%>
		</div>
		<div class="col-sm-4">
			Creado: <%=l.created_at%> 
		</div>
		<br><br>
</div>

</div>

<%end%>